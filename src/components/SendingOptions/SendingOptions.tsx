import * as React from 'react';

import StyledSendingOptions from './StyledSendingOptions';

const SendingOptions: React.FunctionComponent = () => (
  <StyledSendingOptions>Send messages on CTRL+ENTER</StyledSendingOptions>
);

export default SendingOptions;